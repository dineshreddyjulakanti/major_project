# major_project

This repository intentionally tracks only selected assets from three modules:

- `audio`: `.env`, `app.py`, `label_encoder.pkl`, `model_cnn.keras`
- `image`: `app.py`, `final_resnet50_mood.h5`
- `txt`: `app.py`, `modify_bert_model/`

All other files are excluded by `.gitignore`.
